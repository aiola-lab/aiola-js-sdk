export interface AiolaClientOptions {
  apiKey?: string;
  accessToken?: string;
  baseUrl?: string;
  authBaseUrl?: string;
  workflowId?: string;
}

export interface ClientConfig {
  apiKey?: string;
  accessToken?: string;
  baseUrl: string;
  authBaseUrl: string;
  workflowId: string;
}