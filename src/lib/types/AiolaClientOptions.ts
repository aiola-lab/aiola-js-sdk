export interface AiolaClientOptions {
  accessToken: string;
  baseUrl?: string;
  authBaseUrl?: string;
  workflowId?: string;
}

export interface ClientConfig {
  accessToken: string;
  baseUrl: string;
  authBaseUrl: string;
  workflowId: string;
}
